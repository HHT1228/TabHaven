// Copy this file to config.js and fill in your WeatherAPI.com key.
// config.js is gitignored, so your real key stays local and never gets committed.
window.TABHAVEN_WEATHER_API_KEY = 'YOUR_API_KEY';
